<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.delonagames.funnystory">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
            android:name=".FunnyStoryApp"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:usesCleartextTraffic="true">
        <activity android:name=".activities.ShowSentenceHostActivity">
        </activity>
        <activity android:name=".activities.WaitingActivity">
        </activity>
        <activity android:name=".activities.host.HostActivity">
        </activity>
        <activity android:name=".activities.StartNewGameOrConnectActivity">
        </activity>
        <activity android:name=".activities.createsentence.CreateSentenceActivity">
        </activity>
        <activity android:name=".activities.ShowSentenceActivity">
        </activity>
        <activity android:name=".activities.MainSelectVersionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>